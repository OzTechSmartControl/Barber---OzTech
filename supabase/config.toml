# Supabase CLI config — necessário para deploy das Edge Functions
# ID do projeto: encontrado em Supabase → Settings → General → Reference ID
project_id = "kqjzontxfwlwmvbddbnv"

[api]
port = 54321
schemas = ["public", "storage", "graphql_public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 15

[studio]
port = 54323

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["http://localhost:5173"]

[storage]
enabled = true
